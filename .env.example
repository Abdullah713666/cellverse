# Reference only — the application reads environment variables from the host.
# Do not commit real credentials here.

MYSQLHOST=localhost
MYSQLDATABASE=cellverse_db
MYSQLUSER=root
MYSQLPASSWORD=
PORT=8080

# Optional production integrations
RECAPTCHA_SITE_KEY=
RECAPTCHA_SECRET_KEY=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
SMTP_FROM_NAME=CellVerse Admin
